# Canonical product name

The product is **Manifold Agent**.

The core engineering lifecycle/orchestration architecture is **Manifold Agent Native Loop**.

`Manifold Nexus` is an obsolete working name and must not be used for new product, architecture, UI, API or documentation naming.

See `MANIFOLD_AGENT_PRODUCT_BOUNDARY.md` for the full boundary.
